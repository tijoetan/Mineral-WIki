package model.enums;

// Contains the current attributes to sort the mineral table on

import model.entries.WikiEntry;

public enum Attributes {
    CRYSTAL, CLEAVAGE, IOR, HARDNESS, DENSITY, DEFAULT;
}
