package model.tableentry;

import chemicalstructure.Formula;
import model.WikiEntry;

public class FamilyTableEntry extends TableEntry {
    public FamilyTableEntry(WikiEntry entry, Formula chemicalFormula) {
        super(entry, chemicalFormula);
    }
}
